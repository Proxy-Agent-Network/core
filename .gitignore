# --- SENSITIVE DATA ---
.env
*.pem
*.key
proxy_api_secret.txt
# Added to prevent accidental credential leaks
secrets/
config.local.yaml
# Secret Lore and Live Agent Databases
core_memories.json
agency_lore.db
secrets.js

# --- PROVISIONING SECRETS ---
# Prevent Vanguard 50 pilot credentials from leaking
serviceAccountKey.json
*.csv
vanguard_50_credentials.csv

# --- TPM 2.0 HARDWARE (VITAL) ---
# Protecting the Hardware Root of Trust
*.ctx
*.priv
*.pub
*.bin
*.session

# --- PROJECT LOGS ---
*.log
proxy_heartbeat.log
active_tasks.log
# Catch-all for telemetry data
telemetry/

# --- BINARIES & LARGE ASSETS ---
*.psd
*.raw
website/images/raw images/

# --- PYTHON ---
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
.pytest_cache/
.mypy_cache/
*.db

# --- NODE.JS ---
node_modules/
npm-debug.log*
yarn-debug.log*
.npm/

# --- SYSTEM & IDE ---
.vscode/
.idea/
.idea
Thumbs.db
.DS_Store
*.swp
*~

# --- RUST & HYBRID CORE ---
# Huge build artifacts folder
target/
# Backup files
**/*.rs.bk
# Python Wheels (built packages)
*.whl
# Linux Shared Objects (if we cross-compile later)
*.so

# --- L402 / MAINNET CREDENTIALS (NEVER COMMIT THESE) ---
mainnet_secrets/
*.macaroon
*.cert
agent_memory.db
polar_keys/

# --- AGENT GENERATED MEDIA & VECTOR DB ---
nano_banana_*.jpg
nano_banana_*.png
veo_video_*.mp4
lyria_track_*.mp3
lyria_track_*.wav
faiss_index/tests/
hardware-node/mock_boot.py
faiss_index/

# --- MOBILE APP EXCLUDES (iOS/Android) ---
*.iml
.kotlin
.gradle
**/build/
xcuserdata
!src/**/build/
local.properties
captures
.externalNativeBuild
.cxx
*.xcodeproj/*
!*.xcodeproj/project.pbxproj
!*.xcodeproj/xcshareddata/
!*.xcodeproj/project.xcworkspace/
!*.xcworkspace/contents.xcworkspacedata
**/xcshareddata/WorkspaceSettings.xcsettings

# --- ANDROID SIGNING SECRETS ---
# NEVER commit your production app signing keys
*.jks
*.keystore
keystore.properties

# --- REDIS LOCAL DUMPS ---
# Prevents committing the local Redis matching engine database state
*.rdb
dump.rdb