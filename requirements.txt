# =============================================================================
# Proxy Agent Network (PAN) — Python Dependencies
# =============================================================================
# Organized by subsystem. Version pins are intentionally conservative —
# floors (>=) rather than equals (==) wherever possible so the resolver
# doesn't fight with transitive deps.
# =============================================================================

# --- Core Web Framework ---
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
starlette>=0.27.0
pydantic>=2.0.0
python-multipart>=0.0.16

# --- Legacy Flask Stack ---
Flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.0.0
Werkzeug>=3.0.3
Jinja2>=3.1.4

# --- Database & Cache ---
psycopg2-binary>=2.9.9
asyncpg>=0.29.0
redis>=5.0.3

# --- Cryptography & Security ---
cryptography>=41.0.0
ecdsa>=0.18.0
PyNaCl>=1.5.0
PyJWT>=2.8.0
# tpm2-pytss  # Commented out for Windows

# --- Network & External APIs ---
requests>=2.31.0
aiohttp>=3.9.3
httpx>=0.27.0
aioboto3>=12.3.0
boto3>=1.34.0

# --- Google APIs ---
google-auth>=2.29.0
google-api-python-client>=2.124.0
google-genai>=1.55.0

# --- Lightning / gRPC ---
grpcio>=1.60.0
grpcio-tools>=1.60.0
googleapis-common-protos>=1.63.0

# --- Payments & Blockchain ---
stripe>=5.0.0
web3>=6.0.0

# --- Formatting, Parsing & Computer Vision ---
Markdown>=3.4.0
bleach>=6.1.0
Pillow>=10.0.0
opencv-python-headless>=4.8.0
pytesseract>=0.3.10
numpy>=1.24.0

# --- Configuration & Testing ---
python-dotenv>=1.0.0
pytest>=7.4.0

# --- Legacy AI / LLM (Pending Removal) ---
# langchain-google-genai>=1.0.0
# mcp>=1.0.0